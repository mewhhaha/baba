// expect: BRL_TYPE_ASSIGNMENT
// expect: BRL_TYPE_CONDITION
// expect: BRL_TYPE_CAST
fn broken(input: span<u16>) -> bool {
  let offset: u32 = true;
  while offset {
    offset = offset + 1;
  }
  return input as bool;
}
