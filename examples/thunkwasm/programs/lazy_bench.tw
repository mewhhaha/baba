fn expensive x = tick(x + 1);

fn repeat n thunk total =
  if unlikely n == 0 then
    total
  else
    repeat(n - 1, thunk, total + (force thunk));

fn main x =
  let cached = lazy expensive(x) in
  repeat(500, cached, 0);
